#include <bits/stdc++.h>
using namespace std;


void func(string str,int n)
{

    int i=0;
    string::iterator it;
    for(it=str.begin();it!=str.end();it++)
    {
        
    }

}

int main()
{
    string str = "axxzxy";
    int n = str.size();
    func(str,n);
}